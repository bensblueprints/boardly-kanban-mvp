# Boardly configuration
# Copy to .env (or set as environment variables) — all optional.

# Port the server listens on
PORT=5315

# Password for signing in. CHANGE THIS before deploying publicly.
ADMIN_PASSWORD=admin

# Where the SQLite database + attachment uploads live (defaults to ./data)
# DATA_DIR=/var/lib/boardly
