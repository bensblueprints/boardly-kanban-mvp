// Shared by hosted API/ChatGPT agents and the Codex worker.
module.exports = 'GitHub sync is required for every connected project with agent access. After each completed, verified set of authorized source or deliverable changes, commit and push the intended files to the configured repository and branch, even when no deployment is requested. Do this before reporting that change complete or ending the Work assignment. Include new files as well as modifications, preserve existing work and executable modes, and verify the remote branch SHA after the push. Do not create empty commits for read-only work. Never force-push, include secrets or unrelated private files, or overwrite concurrent changes. If a push cannot finish, preserve the changes and report an actionable blocker; do not describe the work as fully synced.';
